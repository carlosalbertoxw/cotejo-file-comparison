/**
 * Alto de una fila en pixeles.
 *
 * Tiene que coincidir exactamente con `--line-height` del CSS: es la constante
 * que ata la rejilla alineada, la numeracion de lineas, las flechas de merge y
 * el mapa de diferencias al alto real de una linea del editor.
 */
export const LINE_HEIGHT = 18
